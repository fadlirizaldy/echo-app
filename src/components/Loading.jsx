import LoadingBarContainer from "react-redux-loading-bar";

const Loading = () => {
  return (
    <div className="top-0 fixed z-20 h-4 w-full">
      <LoadingBarContainer />
    </div>
  );
};

export default Loading;
